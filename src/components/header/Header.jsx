import React from "react";
import { LogoHeader } from "../brand/LogoHeader";

export const Header = () => (
  <header className="site-header">
    <LogoHeader name="ATP" descriptor="Consulting Services" />
  </header>
);
